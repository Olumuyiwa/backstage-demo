# Ignore Git files
.git

# Yarn and npm cache
.yarn/cache
.yarn/install-state.gz
npm-debug.log
.cache
build

# Node modules, to be rebuilt during Docker image build
node_modules
packages/*/node_modules

# # Build artifacts (dist folder generated by yarn build)
# dist

# Local configuration files
*.local.yaml

# Plugins and source files that are not necessary for the Docker image
packages/*/src
plugins
